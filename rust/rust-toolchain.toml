# Chur pins an exact Rust toolchain version per DEVELOPMENT.md ("Do not rely on
# globally mutable defaults"). Raising this pin is a deliberate change reviewed
# like a dependency upgrade, and it must not silently change persisted or wire
# bytes. The supported floor is `rust-version` in Cargo.toml, which is lower.
[toolchain]
channel = "1.97.0"
